{
  "name": "banguide-public",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "build:css": "postcss css/*.css --dir dist/css",
    "prebuild": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\""
  },
  "devDependencies": {
    "cssnano": "^7.0.0",
    "esbuild": "^0.25.0",
    "html-minifier-terser": "^7.2.0",
    "postcss": "^8.4.0",
    "postcss-cli": "^11.0.0"
  }
}
